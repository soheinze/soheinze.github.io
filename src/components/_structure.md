# `src/components/` Structure

This directory contains reusable Astro and UI components.
- Keep them logic-light where possible.
- Use `props` to pass data.
- Ensure styling adheres strictly to the globals defined in `src/styles/global.css`.
