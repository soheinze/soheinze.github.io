# `src/layouts/` Structure

Contains the shell pages that enforce global styling, font imports, and meta tags.
- `Layout.astro`: The primary layout component containing the `<html>`, `<head>`, and `<body>`.
- It sets up the main Astro `<slot />` where page content is injected.
