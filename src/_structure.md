# `src/` Structure

This directory holds all the Astro-specific code.

- **`pages/`**: Only use for entry points and routes.
- **`components/`**: Place pure UI elements here.
- **`layouts/`**: Page wrapper shells.
- **`styles/`**: Global CSS files.
- **`content/`**: Markdown source material.
